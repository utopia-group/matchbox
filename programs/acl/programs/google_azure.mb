tcam google_acl (
    is_inbound : 1,
    srcAddr : 32,
    dstAddr : 32,
    proto : 8,
    l4_sport : 16,
    l4_dport : 16
) : {} [allow deny].
tcam google_ipv4_state (
    is_inbound : 1,
    srcAddr : 32,
    dstAddr : 32,
    proto : 8,
    l4_sport : 16,
    l4_dport : 16
) : {} [seen unseen].

tcam azure_acl (
    is_inbound : 1,
    srcAddr : 32,
    dstAddr : 32,
    proto : 8,
    l4_sport : 16,
    l4_dport : 16
) : {} [allow deny]
o--
    google_acl.

tcam azure_ipv4_state (
    is_inbound : 1,
    srcAddr : 32,
    dstAddr : 32,
    proto : 8,
    l4_sport : 16,
    l4_dport : 16
) : {} [seen unseen]
o--
    google_ipv4_state.
